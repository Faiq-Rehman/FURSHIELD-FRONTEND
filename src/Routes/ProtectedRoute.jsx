import { Navigate, Outlet } from 'react-router-dom'; import { useAuth } from '../Context/AuthContext'; export default function ProtectedRoute({role}){const {user,loading}=useAuth(); if(loading) return <div className="grid min-h-screen place-items-center text-emerald-800">Loading your workspace...</div>; const allowedRole = role === 'owner' ? 'user' : role; return user && (!role || user.role===allowedRole)?<Outlet/>:<Navigate to="/login" replace/>}
